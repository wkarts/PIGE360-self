[project]
name = "pige360-self"
version = "0.3.0"
description = "Secretaria escolar self-hosted — FastAPI e Vue 3"
requires-python = ">=3.13,<3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
